---
slug: errors/E0002
title: E0002 — Not implemented yet
description: A command is declared but its behavior is still a scaffold.
---

> **Storage archive.** Mammoth now focuses on [durable context memory for coding agents](/memory/). This page describes the earlier storage project.


The CLI understood your command, but its implementation does not exist yet.
This is not a broken Rust installation. Command execution returns a nonzero
exit status and explains the missing milestone.

These commands work now:

```bash
cargo run -p mammoth-cli -- --help
cargo run -p mammoth-cli -- --version
cargo run -p mammoth-parts --example 13-block-matrix
```

Run them from the repository root. Follow [Start contributing](/contributing/)
for the beginner guide, the four-person team plan and external contribution steps.
