---
title: Flag reference
description: Every command and every flag, generated from the clap tree and verified in CI.
sidebar:
  order: 5
---

<!-- The body below the "GENERATED" marker is written by `cargo xtask docs`.
     Keep the frontmatter above it — the generator preserves it. CI regenerates
     the file and fails the build if the result differs from what is committed,
     which is what stops this page from drifting away from the binary. -->

This page is the exhaustive one: every command, every flag, every default,
reflected out of the `clap` tree at build time. It is generated, so it is always
right and never explains anything.

For what each command is *for* — and what its output is telling you — read the
hand-written pages instead:

- **[Files](/cli/filesystem/)** — `ls` `put` `get` `cat` `rm` `stat` `du` `df` `find` `setrep` `checksum` …
- **[Visualization](/cli/viz/)** — `viz blocks` `viz cluster` `viz skew` `viz health` `viz flow` `top`
- **[Operations](/cli/operations/)** — `init` `serve` `doctor` `node` `cluster` `admin` `job` `migrate` `bench` `config` `token`

You can also get the same information from the binary, which needs no network:

```bash
mammoth --help
mammoth viz --help
mammoth viz skew --help
mammoth admin balancer --help
```

<!-- GENERATED BY `cargo xtask docs` — DO NOT EDIT BELOW THIS LINE. -->

Run `cargo xtask docs` to populate this section. It is a `todo!()` until
milestone M2 — see [the roadmap](https://github.com/ProjectOrcha/Mammoth/blob/main/docs/ROADMAP.md).
