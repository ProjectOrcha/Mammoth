---
title: Mammoth
description: Durable context memory for coding agents. Save project decisions, conventions, and handoffs locally, then recall them across sessions through MCP.
template: splash
hero:
  tagline: New session. Same memory.
---
import Homepage from '../../components/Homepage.astro';

<Homepage>

```bash
cargo build --release --locked -p mammoth-cli -p mammoth-mcp

./target/release/mammoth memory --project my-app remember pagination \
  --title "API pagination" --kind decision \
  --content "Use cursor pagination for the event log."

./target/release/mammoth memory --project my-app recall "pagination"
```

Requires **Rust 1.88+**. Run from a clone of either branch. Entries persist in
`~/.mammoth/local/agent-memory.sqlite3`. Use `--local-root /absolute/store/path`
before the command to choose another store.

[Connect the same memory to your coding agent →](/memory/mcp/)

</Homepage>
