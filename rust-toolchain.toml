# Pinned to stable; the MSRV floor is `rust-version = "1.82"` in the workspace
# Cargo.toml and is enforced by the `msrv` job in .github/workflows/ci.yml.
[toolchain]
channel    = "stable"
components = ["rustfmt", "clippy"]
